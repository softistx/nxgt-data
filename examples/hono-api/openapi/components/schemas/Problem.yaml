type: object
description: What every refusal answers.
additionalProperties: false
required:
  - message
properties:
  message:
    type: string
    description: A message key, as the applications here use.
    examples:
      - errors.not-found
