type: object
description: |
  What a client sends to change an article: the fields it wants moved, and
  no others. A body that names none of them is allowed and changes nothing
  but the `updatedAt` stamp.
additionalProperties: false
properties:
  title:
    type: string
    minLength: 1
  body:
    type: string
    minLength: 1
