---
---

Example-only: `examples/hono-api` guards `POST /articles` with `@nxgt/redis-guard`, and the example is private and never published; the one line under `packages/` is a comment in `@nxgt/redis-kit`'s `test/`, which is not shipped, so no package changes.
